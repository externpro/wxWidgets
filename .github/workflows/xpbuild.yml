name: Build
permissions:
  contents: read
  pull-requests: write
on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:
jobs:
  linux:
    permissions:
      contents: read
      pull-requests: write
      packages: write
    uses: externpro/externpro/.github/workflows/build-linux.yml@dirtyRepo
    secrets: inherit
    with:
      cmake-workflow-preset: LinuxRelease
  macos:
    uses: externpro/externpro/.github/workflows/build-macos.yml@dirtyRepo
    secrets: inherit
    with:
      cmake-workflow-preset: DarwinRelease
  windows:
    uses: externpro/externpro/.github/workflows/build-windows.yml@dirtyRepo
    secrets: inherit
